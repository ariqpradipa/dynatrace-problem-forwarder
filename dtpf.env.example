# Dynatrace Problem Forwarder Environment Variables
# This file should be placed at: /etc/dtpf/dtpf.env
# Permissions: chmod 600 /etc/dtpf/dtpf.env (protect secrets!)

# Required: Dynatrace API Token
DYNATRACE_API_TOKEN=dt0c01.XXXXXXXXXXXX.YYYYYYYYYYYYYYYYYYYYYYYYYYYY

# Required: Path to configuration file
CONFIG_PATH=/opt/dtpf/config.yaml

# Optional: Logging level (trace, debug, info, warn, error)
RUST_LOG=info

# Optional: Additional connector secrets (referenced in config.yaml with ${VARIABLE})
# WEBHOOK_API_KEY=your-webhook-api-key
# SLACK_WEBHOOK_TOKEN=your-slack-token
# AUTH_TOKEN=your-auth-token
